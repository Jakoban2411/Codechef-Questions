#include <iostream>
#define 
using namespace std;
template<typename T>
struct HashEntries
{
    int key;
    T data;
}
struct HashTable
{
    HashEntries* Nodes;
    bool considerdec=false;
    HashTable(int size)
    {
        Nodes=new HashEntries[size];
        if(size>11)
            considerdec=true;
    }
    void insert(T data)
    {
        int key = data * 0.14159265359
    }
} 
